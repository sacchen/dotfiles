# macOS + editor noise
.DS_Store
*.swp
*.swo

# Local overrides and secrets
**/*.local
**/.env
**/.env.*

# Never commit private keys
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.pem
*.p12
*.key

# Optional generated manifests
manifests/uv-tools.txt

# gh auth material (never commit)
config/.config/gh/hosts.yml
